/**
 * Settings
 *
 * Pattern to allow the import of settings from php with
 * wp_localize_script into editor components.
 */

// global WholesomeExamplesSettings.
const settings = WholesomeExamplesSettings;

export default settings;
